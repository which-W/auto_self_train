# ============================================================
# requirements.txt — Transformer 语言模型训练
# 适用环境：Python 3.11, CUDA 12.1+
#
# 项目仅使用以下第三方包：
#   torch     — 模型训练
#   numpy     — 数据处理
#   wandb     — 实验追踪
#   tokenizers — tokenizer 训练与使用
#   einops    — attention 中的 rearrange 操作
# ============================================================

# PyTorch（CUDA 版本根据你的驱动选择，cu124 兼容 CUDA 12.8）
torch==2.6.0

# 数值计算
numpy>=1.26.0,<3.0.0

# 实验追踪
wandb>=0.19.0

# Tokenizer（huggingface tokenizers）
tokenizers>=0.21.0

# 张量操作工具
einops>=0.8.0
